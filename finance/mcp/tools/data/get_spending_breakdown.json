{
  "$schema": "../../.metabind/sdk/schemas/data-tool.schema.json",
  "description": "Categorized spending + prior-period comparison. Numbers to reason with — to display, use spending_breakdown.",
  "component": "GetSpendingBreakdown",
  "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
  }
}
