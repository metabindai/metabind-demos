# Finance (Android)

Not yet available. `metabind-android` does not publish tagged releases yet, and
demos depend only on published SDK artifacts. This directory will hold a Gradle
project depending on `ai.metabind:metabind-android` once releases exist.
