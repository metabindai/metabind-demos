{
  "$schema": "../../.metabind/schemas/tool.json",
  "config": {
    "allowedDomains": [
      "api.metabind.ai"
    ],
    "annotations": {
      "idempotentHint": true,
      "readOnlyHint": true,
      "title": "Product Lookup"
    },
    "execution": {
      "taskSupport": "forbidden"
    }
  },
  "description": "Fetch one or more Oak&Ivory products by id. Never call this from the LLM. This is only to be called from ui tools. ",
  "rootComponent": "ProductLookup"
}
